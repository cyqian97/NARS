I want you to make the following to modifications on the svg generation:
1. I added a "shadow" polygon in the env svg, use the style of that path to draw the shadow areas.
2. I want you to show gaps at the border of the shadow areas
    a. From the robot position to the start point of the gap (the gap point), draw a line use the style of the "near" path
    b. From the gap point to the endpoint of the gap (the intersection point of the boundary and  the gap line extended from the gap point), draw a line use the stype lf the "far" path