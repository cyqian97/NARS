 I want to simplified and refactor the code:
  1. separate the backend from the frontend. The backend contains processing and computing the geometric graphs and lines, and the frondend
  is the pygame part, including all the mouse/keyboard interactions and save/load functions. I want to make the backend like an API with 
  functions I can call for other purposes, e.g. generate svg figures.
  2. simplify the backend part. For this part, as  