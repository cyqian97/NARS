name: NARS
channels:
  - defaults
dependencies:
  - python=3.12
  - numpy
  - pip
  - pip:
    - pygame==2.5.2
    - tqdm==4.66.2
    # pyvisgraph is included in the repository (local package), no install required
